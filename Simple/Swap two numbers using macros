#include <iostream>
#include<math.h>
#define SWAP(a, b) {int temp; temp = a; a = b; b = temp;}
using namespace std;

int main() {
  int x, y;
  cin >> x >> y;
  
  cout << "x: " << x << " " << "y: " << y << endl;
  SWAP(x, y);
  cout << "x: " << x << " " << "y: " << y << endl;
return 0;
}
